// // Sum numbers:
// let numbers = [1, 2, 3, 4, 5];

// function sumArray(arr) {
//   // Base case: empty array
//   if (arr.length === 0) return 0;

//   // Recursive case: Non-empty array
//   const [first, ...rest] = arr; // Get first element, and rest of array
//   const remainingElements = sumArray(rest); // Pass rest of array to recursive call
//   return first + remainingElements; // Combine sum of first element with sum of remaining elements
// }

// console.log(sumArray(numbers)); // 15

// ===========================================================================================

// Reverse string:
function reverseString(str) {
  // ???
}

module.exports = reverseString;
